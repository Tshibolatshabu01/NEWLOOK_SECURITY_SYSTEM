# V12 bootstrap contract

The four existing applications remain the actual entrypoints. New enterprise code must import from `SaaS/core` for new functionality rather than creating a second Firebase/tenant/RBAC implementation.
